# Como instalar e executar o LINKTRAC PRIME

## Pré-requisitos

- Node.js instalado
- Conta no MongoDB Atlas

## Passo a Passo

1. Clone o repositório
2. Configure o arquivo `.env` na pasta `backend` com sua conexão do MongoDB
3. No terminal:

### Backend

```bash
cd backend
npm install
npm run dev
```
